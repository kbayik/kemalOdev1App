<title>My application deployed automatically</title>
  <h1>Hello, World!</h1>

    In the first form, enter a list of integers seperated by a newline character.
    In the second form, enter a list of integers seperated by a newline character.
    In the third form, enter an integer.

    If this integer is found on both lists, result becomes 3.
    If this integer is found on only second list, result becomes 2.
    If this integer is found on only first list, result becomes 1.
    If this integer is not found, or lists are empty, result becomes 0.

    Result is {{result}}


  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input2" rows="10" cols="30"></textarea>
        <textarea name="input3" rows="10" cols="30"></textarea>
        <br>
        <input type="submit">
  </form>
